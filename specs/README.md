# Specs 规格说明书

## 理念

每个功能一个规格，**只写即将开发的部分**。规格书 1-2 页，用完即弃，不必追求完整。

## 流程

1. 开发新功能前，先写 `specs/feature-name.spec.md`
2. 按规格实现代码
3. 运行 `npm run ci:enhance` 验证
4. 规格书随代码保留，后续修改同步更新

## 模板

见 `template.spec.md`
